package com.springboot.notification.route;

public interface RouteConstant {
    interface APP {
        interface NOTIFY {
            String NOTIFY = "/app/notify";
            String SEND = "/app/notify/send";
        }
    }
}
